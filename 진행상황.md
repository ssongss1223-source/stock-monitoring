# 진행상황 (세션 인수인계 파일)

> Claude가 세션 시작 시 이 파일을 먼저 읽어 컨텍스트를 복원한다.
> 작업 완료 시마다 업데이트할 것.

---

## 프로젝트 한 줄 요약

매일 16:00 장 마감 후 국내 주식 기술적 분석 → 매수추천(S/A/B) + 매도신호 → 텔레그램 알림.
완전 자동매매 아님. 의사결정 지원 시스템.

---

## 현재 상태

**Phase**: Phase 1 완료 + 패턴 학습 모듈 구현 완료

**마지막 작업일**: 2026-05-05 (세션 5)

**다음 할 일**: 패턴 학습 검증 → GCP 배포
1. ✅ `.env` 파일 생성됨 (KRX_ID/PW 포함, 텔레그램 토큰 비어있음)
2. ✅ `python scripts/verify_agents.py` — 에이전트별 단독 실행 검증
3. ✅ `python main.py --run-now` — 파이프라인 정상 동작 확인
4. ✅ 텔레그램 토큰/채팅ID 입력 후 실제 메시지 수신 확인
5. ✅ 2025-04 폭락기 데이터로 market_bias=+5 확인
6. `python backtest/pattern_backtest.py --ticker 005930` — 패턴 백테스트 검증
7. `python main.py --run-now` — 패턴 학습 통합 후 전체 파이프라인 재확인
8. GCP 배포 (deploy/startup.sh → VM 세팅 → systemd → Secret Manager)

---

## 핵심 설계 결정 사항 (요약)

| 결정 | 내용 |
|---|---|
| 멀티에이전트 | 8개 에이전트, Python asyncio 기반 병렬 실행 |
| 유연한 스코어링 | 모든 점수 규칙을 YAML 외부화 (`config/scoring/v1_baseline/`) |
| 하락장 처리 | 매수 차단 아님 → `market_bias` 페널티(+5)로 기준 높임 |
| 종목 유니버스 | 섹터별 상위 5개(기본) or 시총50 or 관심종목 (config.py 설정) |
| GCP 배포 | e2-micro VM + APScheduler + systemd (무료 티어) |
| LLM 사용 | Phase 1: 없음 / Phase 2: claude-haiku-4-5 (뉴스 감성) |

---

## Phase 1 작업 체크리스트

### 인프라/코어
- [x] `core/scoring_engine.py` — ScoringEngine 클래스 (YAML 로드 + 7개 메서드)
- [x] `config/scoring/v1_baseline/` — 5개 YAML (market/volume/technical/buy_grade/sell)
- [x] `models/signals.py` — MarketContext, TechnicalResult(+current_price), VolumeResult, BuySignal, SellSignal
- [x] `config.py` — UNIVERSE_MODE, SCORING_VERSION, 텔레그램 설정
- [x] `data/portfolio.json`, `data/watchlist.json` — 초기 빈 파일
- [x] `requirements.txt`
- [x] `.env.example`

### 에이전트
- [x] `agents/market_filter.py` — MarketFilterAgent (KOSPI/KOSDAQ 독립 분석, 섹터비율 포함)
- [x] `agents/universe_manager.py` — UniverseManager (sector_top5/top50_mktcap/watchlist)
- [x] `agents/volume_analysis.py` — VolumeAnalysisAgent (OBV, 수급, 공매도 포함)
- [x] `agents/technical_analysis.py` — TechnicalAnalysisAgent (MA+일목+패턴4종+지지저항)
- [x] `agents/buy_signal.py` — BuySignalAgent (앙상블 등급 + 손절/목표가 계산)
- [x] `agents/sell_signal.py` — SellSignalAgent (강제손절/익절/추세이탈/억제 스코어링)
- [x] `agents/report.py` — ReportAgent (전략서 12장 포맷, 텔레그램 발송)
- [x] `agents/orchestrator.py` — Orchestrator (asyncio 파이프라인, 동시성 8)
- [x] `main.py` — APScheduler 진입점 (--run-now 플래그 지원)

### 검증
- [x] `.env` 파일 생성 (KRX_ID/PW 설정완료, 텔레그램 미설정)
- [x] `scripts/verify_agents.py` 작성 완료
- [x] `python main.py --run-now` — 파이프라인 정상 동작 확인 (매수신호 생성됨)
- [x] 텔레그램 토큰/채팅ID 입력 후 실제 메시지 수신 확인 (2026-05-05 완료)
- [x] 2025-04 폭락기 데이터로 market_bias=+5 확인 (2026-05-05 완료 — KOSPI/KOSDAQ 모두 BEAR+5 발동)
- [ ] `python backtest/pattern_backtest.py --ticker 005930` — HIGH 정밀도 >60% 확인
- [ ] 패턴 학습 통합 후 `python main.py --run-now` 재검증 (📊 패턴 분석 섹션 텔레그램 출력 확인)

### 배포
- [ ] `deploy/startup.sh` — GCP VM 초기 설치
- [ ] `deploy/update.sh` — 코드 업데이트 스크립트
- [ ] GCP e2-micro VM 세팅
- [ ] systemd 서비스 등록
- [ ] Secret Manager 설정 (텔레그램 토큰)

### 백테스트
- [ ] `backtest/run_backtest.py` — vectorbt 기반
- [ ] 2025-04 ~ 2026-04 검증

---

## Phase 2 작업 (추후)
- [ ] `agents/sentiment.py` — SentimentAgent (뉴스/DART/유튜브)
- [ ] MarketFilterAgent에 매크로 센티멘트 추가
- [ ] scoring v2_* YAML 버전 생성

---

## 파일 구조 현황

```
stock_monitoring/
├── 주식_신호_알림_시스템_전략서.md   ✅ 존재
├── 설계문서.md                        ✅ 존재
├── 진행상황.md                        ✅ 이 파일
├── agents/
│   ├── market_filter.py               ✅ MarketFilterAgent
│   ├── universe_manager.py            ✅ UniverseManager
│   ├── volume_analysis.py             ✅ VolumeAnalysisAgent
│   ├── technical_analysis.py          ✅ TechnicalAnalysisAgent
│   ├── buy_signal.py                  ✅ BuySignalAgent
│   ├── sell_signal.py                 ✅ SellSignalAgent
│   ├── report.py                      ✅ ReportAgent (패턴 섹션 추가)
│   ├── orchestrator.py               ✅ Orchestrator (패턴 학습 통합)
│   └── pattern_learning.py           ✅ StockPatternLearner (신규)
├── core/
│   └── scoring_engine.py              ✅ ScoringEngine
├── models/
│   └── signals.py                     ✅ 5개 dataclass
├── data/
│   ├── portfolio.json                 ✅ 초기 빈 파일
│   ├── watchlist.json                 ✅ 초기 빈 파일
│   └── pattern_cache.json             ✅ 초기 빈 파일 (종목별 최적 윈도우 캐시)
├── config/scoring/v1_baseline/        ✅ 5개 YAML 파일
├── deploy/                            ✅ 디렉토리 생성 (파일 미생성)
├── backtest/
│   └── pattern_backtest.py            ✅ 패턴 백테스트 CLI
├── config.py                          ✅ 존재
├── main.py                            ✅ APScheduler + --run-now 플래그
├── requirements.txt                   ✅ 존재
└── .env.example                       ✅ 존재
```

---

## 이번 세션(2026-05-05 세션5) 패턴 학습 모듈 구현

`staged-discovering-pumpkin.md` 설계 기반으로 전체 구현 완료.

| 파일 | 변경 내용 |
|---|---|
| `models/signals.py` | `PatternLearningResult` dataclass 추가 (8개 필드) |
| `agents/pattern_learning.py` | **신규** — StockPatternLearner (윈도우 최적화, 4채널 코사인 유사도, 등급 판정, 캐시) |
| `agents/technical_analysis.py` | `self.last_df` 저장 (OHLCV 재사용, 추가 API 호출 방지) |
| `agents/orchestrator.py` | 패턴 학습 단계 통합, `_analyze_stock` 반환 타입 → `tuple[BuySignal\|None, PatternLearningResult]` |
| `agents/report.py` | `_pattern_section` 추가, `send()` `pattern_results` 파라미터 확장 |
| `backtest/pattern_backtest.py` | **신규** — CLI 백테스트 도구 (`--ticker`, `--train-days`) |
| `data/pattern_cache.json` | **신규** — 종목별 최적 윈도우 일별 캐시 |

**알고리즘 요약:**
- 4채널 (가격수익률·거래량비율·외국인순매수·기관순매수) → 2채널 fallback
- 종목별 최적 윈도우 그리드 서치 (W=10/15/20/30/40), 결과 일별 캐싱
- top-K=20 코사인 유사도 → 성공률 기반 HIGH/MEDIUM/LOW/INSUFFICIENT 등급
- 기존 점수와 별도로 텔레그램 📊 섹션으로 출력

---

## 이번 세션(2026-05-05) 버그픽스 내용

| 파일 | 수정 내용 |
|---|---|
| `main.py` | `_PykrxNoiseFilter.filter()` — pykrx `logging.info(args, kwargs)` 호출 시 TypeError 발생 방지 (try/except 추가) |
| `agents/orchestrator.py` | `asyncio.gather(*tasks, return_exceptions=True)` — 개별 종목 실패가 전체 파이프라인 중단 방지 |
| `agents/orchestrator.py` | `_analyze_stock()` — `asyncio.wait_for(..., timeout=20.0)` 추가 (pykrx API 무한 대기 방지) |
| `agents/orchestrator.py` | `_CONCURRENCY = 8 → 2` — KRX rate-limit 방지 |

**검증 결과 (2026-05-05):**
- 290종목 분석, 109종목 매수 신호 (A급 22개, B급 87개)
- 총 소요 시간: ~2분
- "=== 일일 분석 완료 ===" 로그 확인
- 텔레그램 포맷 정상 출력 (콘솔 대체)

---

## 이번 세션(2026-05-04) 구현 내용

| 파일 | 주요 구현 내용 |
|---|---|
| `core/scoring_engine.py` | YAML 로드, 장세/거래량/기술/매수등급/매도 스코어링 메서드 7개 |
| `config/scoring/v1_baseline/*.yaml` | 전략서 수치 그대로 5개 YAML (전략서 4·5·6·9·11장) |
| `models/signals.py` | MarketContext, TechnicalResult(current_price 포함), VolumeResult, BuySignal, SellSignal |
| `config.py` | 환경변수 로드, UNIVERSE_MODE/SCORING_VERSION 설정 |
| `agents/market_filter.py` | KOSPI/KOSDAQ 독립 분석, 섹터 강세 비율, 외국인 순매수 (Phase 1 근사값) |
| `agents/universe_manager.py` | sector_top5(pykrx 섹터 인덱스 포트폴리오) / top50_mktcap / watchlist 모드 |
| `agents/volume_analysis.py` | OBV, 거래량 연속증가, 횡보+거래량, 52주 저점 대비, 수급, 공매도 |
| `agents/technical_analysis.py` | MA 8개 + 일목균형표 + 패턴 4종(BB수축/삼각수렴/밥그릇/컵앤핸들) + 지지저항 |
| `agents/buy_signal.py` | 앙상블 등급 산출 + 손절가(-5% or 지지선) + 목표가(저항선 or +10%) + 손익비 |

### ScoringEngine 단위 테스트 결과 (smoke test 통과)
- 상승장: score=8, bias=0, bull ✅
- 거래량 폭발: score=17, explosion=True ✅
- 기술 total=18 (MA=13, 일목=5) ✅
- 상승장 S등급, 하락장(bias=5) S등급(score=18≥17) ✅
- 강제 손절 감지 ✅

---

## 주요 파일 참조

| 파일 | 용도 |
|---|---|
| `설계문서.md` | 아키텍처 전체 설계 (에이전트 역할, YAML 구조, GCP 배포) |
| `주식_신호_알림_시스템_전략서.md` | 전략 원문 (스코어링 규칙 수치 출처) |
| `config/scoring/v1_baseline/*.yaml` | 스코어링 규칙 (코드 수정 없이 파라미터 변경) |
| `data/portfolio.json` | 보유 종목 + 매수가 `{"005930": 70000}` |
| `data/watchlist.json` | 관심종목 `{"stocks": ["005930", ...]}` |

---

## GCP 환경 정보

| 항목 | 값 |
|---|---|
| 인스턴스 타입 | e2-micro (무료 티어) |
| 리전 | us-central1 |
| 스케줄 | 매일 07:00 UTC = 16:00 KST |
| 실행 방식 | systemd 서비스 + APScheduler |
| 민감 정보 | GCP Secret Manager |

---

## 세션 재개 방법

새 세션 시작 시 Claude에게:
1. 이 파일(`진행상황.md`) 읽기 요청
2. `설계문서.md` 읽기 (필요 시)
3. 체크리스트에서 다음 미완료 항목부터 시작

---

*마지막 업데이트: 2026-05-05 세션5 — 패턴 학습 모듈(staged-discovering-pumpkin.md) 전체 구현 완료. 다음: 백테스트 검증 → GCP 배포*
